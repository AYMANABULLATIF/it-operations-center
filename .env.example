# Optional: enable the Gemini-powered checklist assistant.
# The app still works without this key and will show local fallback checklists.
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: override the SQLite database path.
# DATABASE_PATH=it_operations_center.db
